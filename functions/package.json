{
  "name": "punjabi-app-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check lib/config.js && node --check lib/speech-app.js"
  },
  "dependencies": {
    "@google-cloud/text-to-speech": "^6.3.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  }
}
